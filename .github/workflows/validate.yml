name: HACS validation
on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  hacs:
    name: HACS Action
    runs-on: 'ubuntu-latest'
    steps:
      - name: HACS Action
        uses: 'hacs/action@22.5.0'
        with:
          category: 'plugin'
